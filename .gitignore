# ─── Python ───
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ─── Данные приложения (создаются автоматически при запуске) ───
# uploads — загруженные видео; cache — кеш анализа; tmp — временные;
# output — готовые монтажи; thumbs — превью. Всё это — локальные данные,
# в репозиторий не попадают. Приложение создаёт эти папки само.
data/

# ─── Редакторы и ОС ───
.vscode/
.idea/
.DS_Store
Thumbs.db
